**push** stages everything(1) in the current working directory for a `git` commit, commits with the generic message "autopush," and pushes the commit upstream. 

(1) note that every `git commit` is controlled by the `.gitignore` file, so "everything" actually means "whatever is allowed to be committed by `.gitignore`.
